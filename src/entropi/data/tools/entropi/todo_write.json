{
  "name": "todo_write",
  "description": "Manage the task list for planning and tracking work.\n\nUse this tool to:\n- Create an implementation plan (list of pending tasks for another tier to execute)\n- Track progress on a multi-step task you are executing\n\nWhen planning for another tier, leave all items as `pending` — the executing tier marks progress.\n\nWhen executing, mark the current item `in_progress` before starting and `completed` after finishing. Only ONE item `in_progress` at a time.\n\nTask states:\n- pending: Not started yet\n- in_progress: Currently working on (only ONE at a time)\n- completed: Finished successfully\n\nUse imperative form for content (\"Fix bug\") and active form for display (\"Fixing bug\").",
  "inputSchema": {
    "type": "object",
    "properties": {
      "todos": {
        "type": "array",
        "description": "The complete updated todo list",
        "items": {
          "type": "object",
          "properties": {
            "content": {
              "type": "string",
              "description": "Task description in imperative form"
            },
            "active_form": {
              "type": "string",
              "description": "Task description in present continuous"
            },
            "status": {
              "type": "string",
              "enum": ["pending", "in_progress", "completed"]
            }
          },
          "required": ["content", "status", "active_form"]
        }
      }
    },
    "required": ["todos"]
  }
}
